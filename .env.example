KIMI_K2_API_KEY=
CHAT_API_KEY=
STT_AI_API_KEY=

PORT=7200
DATABASE_URL=postgresql://takumiai_agent:your-secure-password@localhost:7432/takumiai_agent_db
VALKEY_URL=redis://localhost:7379

# x402 Phase 5 test harness (spec §9) — URL of the throwaway demo
# seller's security due-diligence oracle. When set, Core's prompt instructs the
# agent to x402_fetch this resource during DeFi safety/rebalance.
# Leave UNSET in production (provider-neutral, SI-7).
# Example (demo seller default port 4021):
# X402_SECURITY_AUDIT_URL=https://<seller-origin>/api/v1/security-audit
